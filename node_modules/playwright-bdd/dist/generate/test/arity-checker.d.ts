/**
 * Validates step definition arity against runtime invocation shape and
 * accumulates all mismatches found while generating a feature test file.
 */
import { PickleStep, Step } from '@cucumber/messages';
import { MatchedStepDefinition } from '../../steps/matchedStepDefinition';
import { BDDConfig } from '../../config/types';
export declare class ArityChecker {
    private featureUri;
    private config;
    readonly errors: string[];
    constructor(featureUri: string, config: BDDConfig);
    checkStepDefinitionArity(matchedDefinition: MatchedStepDefinition, pickleStep: PickleStep, gherkinStep: Step): void;
    private getExpectedArgs;
    private shouldCheckArity;
    private formatError;
}
//# sourceMappingURL=arity-checker.d.ts.map